import chai from 'chai';
import {None, Some} from '../src/option';

chai.should();

describe('blah', function() {
    it('blah', function() {
        let x = for {
            a <= new Some(1);
        }
    });
});

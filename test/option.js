import chai from 'chai';
import {None, Some} from '../src/option';

chai.should();

describe('Option', function() {
    it('foo', function() {
        let x = for {
            a <= new Some(1);
        }
    });
});
